#include<iostream>
#include<cstring>
using namespace std;
typedef int dataType;

typedef struct Node{
    dataType data;
    struct Node *next;
}LinkNode;

LinkNode* initList()
{
    LinkNode *head, *p, *q;
    int n;
    printf("请输入链表长度:");
    scanf("%d", &n);
    while(n--)
    {
        
    }
}

int main()
{
    cout<<"test"<<endl;
    return 0;
}
